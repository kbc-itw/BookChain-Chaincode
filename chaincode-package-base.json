{
  "name": "BookChain-Chaincode-${chaincodeName}",
  "version": "1.0.0",
  "description": "BookChain Chaincode script for ${chaincodeName}",
  "main": "chaincode/${chaincodeName}.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node chaincode/${chaincodeName}.js"
  },
  "author": "kbc-itw",
  "license": "MIT",
  "dependencies": {
    "fabric-shim": "^1.1.0-alpha",
    "source-map-support": "^0.5.3"
  }
}
